---
  name: "test_traffic_sensor_rule"
  enabled: true
  description: "Test Traffic Sensor"

  trigger:
    type: vdx_sensor.matched_no_traffic

  criteria: {}

  action:
    ref: core.local_sudo
    parameters:
      cmd: echo "`date \"+%Y-%m-%d %H:%M:%S\"` {{trigger.interface_name}} with {{trigger.interface_stats}}" >> /home/vagrant/test_traffic_sensor.out
